apiVersion: v1
kind: Pod
metadata:
  name: frontend-pod
  namespace: frontend
  labels:S
    app: frontend
spec:
  containers:
    - name: frontend
      image: your-docker-registry/frontend:latest
      ports:
        - containerPort: 3000
